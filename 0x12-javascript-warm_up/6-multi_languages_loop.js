#!/usr/bin/node

// prints 3 lines: (like 1-multi_languages.js) but useing ARRAY.
// using an array of string loop

const array-js = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(array-js[i]);
}
